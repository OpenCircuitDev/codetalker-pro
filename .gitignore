# Gradle
.gradle/
build/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Android SDK / NDK
*.apk
*.ap_
*.aab
*.dex
proguard/

# Kotlin
.kotlin/

# Local AARs (XREAL Nebula SDK is large; download separately)
app/libs/*.aar
app/libs/*.jar
!app/libs/.gitkeep

# Logs
*.log

# OS
.DS_Store
Thumbs.db
local.properties

# Release signing — never commit a keystore or the file holding its
# password. The release procedure lives in docs/DEVELOPER-GUIDE.md.
*.keystore
*.jks
keystore.properties
release-keystore.properties

# Sentry secrets — DSN goes in BuildConfig via signing config, not in
# the source tree.
sentry.properties
